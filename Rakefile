#require "bundler/gem_tasks"
#require "rspec/core/rake_task"

#RSpec::Core::RakeTask.new(:spec)

#task :default => :spec

task :default => :tu

desc "Pruebas unitarias pa la práctica 5"
task :tu do 
	sh "ruby -I. test/tc_p5.rb"
end

